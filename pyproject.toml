[tool.poetry]
name = "repl_python3_beta_keysel"
version = "0.1.0"
description = ""
authors = ["KunalDandekar <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
getch = "^1.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
